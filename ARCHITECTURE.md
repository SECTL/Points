# Points 项目架构文档

## 1. 架构概述

本项目采用严格的**自上而下、分层隔离**的架构设计。核心原则是：上层模块可以调用下层模块，但**禁止跨层调用或反向依赖**。

目前项目处于**早期开发阶段**，部分层级仅有接口定义（头文件）或尚未实现。

| 层级 | 模块文件 | 职责 | 当前状态 |
| :--- | :--- | :--- | :--- |
| **第一层：交互层** | `user_interface.h` <br> `console.h` | 负责与用户进行交互。目前为控制台界面，未来将替换为 `Slint` UI 框架。 | **未动工** |
| **第二层：业务逻辑层** | `business_logic.h` | 实现积分管理的核心业务功能，如：<br>- 学生、规则、礼品的增删改查<br>- 积分增减与排名计算<br>- 业务规则校验 | **未动工** |
| **第三层：数据处理层** | `data_processing.h` | 作为业务逻辑与数据存储之间的**转换与适配层**。负责：<br>- 将业务层传递的 `Student`, `Rule`, `Gift` 实体转换为存储格式<br>- 将存储层读取的原始数据转换为业务实体 | **仅有头文件声明**，无实现 |
| **第四层：数据存储层** | `data_storage.h` <br> `internet_apply.h` | - `data_storage.h`：负责本地二进制文件的读写（具体实现细节请自行阅读代码或询问chenjintang-shrimp￼）<br>- `internet_apply.h`：负责与网络相关的数据更新操作 | **已实现**|
| **第五层：交互层** | `entity.h`| **不属于任何特定层**，各层均可使用。 | **已实现** |

## 2. 层间交互规范

1. 调用路径：交互层 -> 业务逻辑层 -> 数据处理层 -> 数据存储层。
2. 数据传递：上层通过调用下层接口，并传递 entity.h 中定义的结构体或基础类型进行通信。
3. 隔离性：各层仅通过头文件暴露的接口函数进行交互，内部实现细节对外部完全隐藏。

## 3. 未来开发路线

- 短期：开发 business_logic.h 中的核心业务功能。
- 中期：实现 data_storage.h (二进制读写) 和 data_processing.h (数据转换) 的具体逻辑。
- 长期：将控制台交互 (console.h) 迁移至 Slint UI 框架，实现图形界面。